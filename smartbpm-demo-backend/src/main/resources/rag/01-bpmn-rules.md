# BPMN rules
- Every process must have exactly one start event and at least one end event.
- Use exclusive gateways when a decision splits the flow into mutually exclusive paths.
- Keep task names in verb + object form.
- Do not leave orphan nodes without incoming or outgoing flow.
- Keep layouts left-to-right and avoid overlapping labels.
